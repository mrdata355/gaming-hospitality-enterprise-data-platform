name: gaming_hospitality_analytics
version: '1.0.0'
config-version: 2
profile: gaming_hospitality_bigquery
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]
models:
  gaming_hospitality_analytics:
    staging:
      +materialized: view
      +schema: stg
    marts:
      +materialized: incremental
      +schema: mart
      +on_schema_change: sync_all_columns
